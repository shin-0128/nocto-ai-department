# HANDOFF — デスクトップで続きをやるための記録

作成: 2026-07-08（ノート側セッション）／次の作業者: Shin（デスクトップ）＋ Claude

---

## 0. まず何が起きたか（30秒）

- 7/9 の建築業クライアント面談用に、Nocto AI Department の2ツールを作った。
- 元は `nocto-agents` repo PR #1 に入れたが、**AI Department を独立プロジェクト**として
  `C:\src\ai-department` に切り出した（このフォルダ）。まだ GitHub remote は無い（Shinが作る）。
- iPad は使わない方針に変更（PC実演でOK）。

## 1. デスクトップでの立ち上げ手順

```powershell
# ① このフォルダを GitHub に上げる（Shinがやる。ノート側で git init + 初期commit 済み）
cd C:\src\ai-department
gh repo create nocto-ai-department --private --source=. --remote=origin --push
#   ↑ repo名は好みで。private 必須。

# ② デスクトップで clone
cd C:\src
git clone https://github.com/shin-0128/nocto-ai-department.git ai-department

# ③ 動作確認（ビルド不要・ダブルクリックで開くだけ）
#   diagnose/ai-shain-jitsuen.html … 実演デモ（入口）
#   diagnose/ai-agent-diagnosis.html … 診断ツール
```

> 依存パッケージ無し。npm/pip 不要。純粋な単一HTML×2。

## 2. JARVIS（MCP）認証について

- 今セッションで JARVIS コネクタが認証切れ→再接続中で、`jarvis_remember/recall` が使えなかった。
- **再認証**: claude.ai のコネクタ設定、または対話セッションで `/mcp` から `jarvis` を繋ぎ直す。
- 認証できたら、この面談準備の要点を `jarvis_remember` に流し込むこと（下記「記憶に残す要点」）。
- なぜ切れたか: セッション側のOAuthトークン期限切れ（非対話セッションでは再取得不可）。恒久対策は
  デスクトップの対話セッションで一度 `/mcp` 認証し直せば安定する。

## 3. いま完成しているもの（受け入れ確認済み）

### 実演デモ `diagnose/ai-shain-jitsuen.html`（★入口・最重要）
写真投入 → AIの処理を1ステップずつ実況（工種判定/黒板OCR/台帳振り分け・確信度・スキャン演出）
→ 工種別に写真台帳が自動完成 → 低確信度2件だけ「要確認」で正直に提示（＝信頼の見える化）
→ 人はタップで承認 → 効果可視化（手作業3h→AI42秒+確認2分/年約280h削減）→ 診断ツールへCTA。
- AI社員キャラ「事務のジロー」。決め台詞「ITは苦手なままでいい。あなたは確認して承認するだけ」。
- **現状は型（サンプル固定12枚）**。実写真解析は未実装。
- headless Chrome で全4ステージ実レンダリング確認済み。

### 診断ツール `diagnose/ai-agent-diagnosis.html`
7業態プリセット（建築総合/リフォーム/設備工事/現場監督/土木/飲食/汎用）。業務棚卸し→AI社員自動
当て込み→回収月数試算→A4印刷。削減額≤0時のフォールバック文言・inputmode=numeric・助成金記入欄
（御社名/作成日/作成:Nocto,Inc.）実装済み。回収月数の検算・A4 1枚・オフラインを実機確認済み。

## 4. 次の一手（Shinに選んでもらう途中で中断した）

面談で効く順の候補（俺=Claudeの推しは①）:

1. **自分の写真で試す（アップロード）** ← 推し。Shin元の指示「写真入れたら」を文字通り。
   客が自分の現場写真をその場でドロップ→見える化パイプラインが自分の写真の上で動く。入口の破壊力最大。
   - 設計上の判断ポイント: 実写真の工種を誤ラベルすると職人に一発でバレる。
     → 対策案＝実アップ時は「要確認」を多めに出し、"AIが自信ない所は必ず人に聞く"機能として見せる
       （誤りを弱点でなく信頼の演出に変える）。ここはShinの営業リスク許容度の判断が要る。
2. **2つ目の業務の実演（見積 or 電話番）**。CTAで「見積・電話番も同じ」と言ってる裏付け。
3. **面談スクリプト/営業トーク1枚**（競合比較の一言・価格の出し方・補助金・想定反論への切り返し）。
4. **evidence数値の裏取り**（KENTEM 384h/積算1週間→1-2日/施工計画書85%削減 等をweb一次情報で検証）。
   面談で数字を出すなら誤りは致命的。マーケチームで事実確認。

## 5. 記憶に残す要点（JARVIS認証後に jarvis_remember へ）

- AI Department＝中小(まず建築)に「AI社員」を月額+初期で配備。SaaSでもBPOでもない第3の売り方。
- Shin確定の設計思想: ITアレルギーは緩和せず**見える化で突破**（絶対条件）。人は確認と承認だけ。
- 価格・時給・削減時間は全部仮（各HTMLのCONFIG集約）。補助金は建築いける想定（年商2500万↑・IT補助未受給層）。
- 面談前の宿題: ①接客5万の中身定義 ②単体SaaSとのバラ比較で割高問題→パッケージ内訳の見える化
  ③Noctoが登録IT導入支援事業者になれるか/代行型が補助対象か確認。
- 出自: nocto-agents PR #1（jarvis/20260707-104040）→ C:\src\ai-department に独立。

## 6. 競合調査の結論（competitorエージェントより・要点）

- ポジションは空いている: 「地方建設中小 × AI社員を"雇う" × 設定済みで配属し運用まで持つ」の掛け合わせ。
  近い競合 marutto は建設特化でない、燈/日立はツール型（自分で使う）、SaaS群は運用を客に投げる。
- 最大リスク=単体SaaS比較で割高に見える。対策=パッケージ内訳の見える化＋補助金で初期を実質1/4＋
  人間BPO（240h=72万）と金額で並べる。
- 差別化の一言（叩き台）:「道具を"買って自分で運用"でも、人を"雇って教育"でもなく、事務も電話も
  集客も"設定済みのAI社員"として配属して運用まで持ちます。導入費は補助金で最大3/4戻ります。」
